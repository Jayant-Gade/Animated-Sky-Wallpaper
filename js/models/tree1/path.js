// js/models/tree1/path.js
/*
import * as C from "./const.js";

// This function returns the fixed coordinates for this tree's base.
function getPosition() {
  const x = window.innerWidth * C.BASE_X_PERCENT;
  // We calculate the Y position from the bottom to make it responsive.
  // The Y coordinate is the base minus the display height, so it sits on the ground.
  const y = window.innerHeight * C.BASE_Y_PERCENT - C.DISPLAY_HEIGHT;
  return { x, y };
}
//
// There is no moveOnPath function needed.
// We export the calculator so the init function can get the starting position.
export { getPosition as getPositionOnPath };
*/

//as no path is there to track
